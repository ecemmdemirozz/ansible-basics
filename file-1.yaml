---
  - name: Playbook-line-in-file.yaml
    hosts: localhost
    tasks:
      - name: Create file1
        lineinfile: 
          dest: /tmp/file1
          line: '{{ item }}'
        with_items:
          - This is line 1
          - This is line 2
     
            
              
          

